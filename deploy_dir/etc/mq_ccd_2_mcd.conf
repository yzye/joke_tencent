<root>
		 shm_key = 287454042
		 shm_size = 256000000
#		 sem_key = 287454020
#		 sem_index = 0
#		 sem_size = 8
		 #dequeue的时候是否加锁，当只有一个进程dequeue时可以不加，否则需要加锁
		 sem_rlock = 0
		 #enqueue的时候是否加锁，当只有一个进程enqueue时可以不加，否则需要加锁
		 sem_wlock = 0
		 #posix信号量名字，在/dev/shm下创建
		 sem_name = /joke_test1
		 fifo_path = ../fifo/joke_mq_ccd_2_mcd.fifo
		 wait_sec = 0
		 wait_usec = 10
</root>
